            <!-- login form -->
            <form class="container" id="loginForm">
                <h3>Login Here</h3>
                <div class="row columns">
                    <label>Email
                        <input class="loginEmail inputField" type="text" name="keyword" placeholder="Enter here">
                    </label>
                </div>
                <div class="row columns">
                    <label>Password
                        <input class="loginPassword inputField" type="password" name="keyword" placeholder="Enter here">
                    </label>
                </div>
                <button type="submit" class="primary button expanded search-button">
                    Submit
                </button>
            </form>